/**
 * Stub for static asset imports (images, SVGs, etc.).
 *
 * Jest cannot process binary files, so we replace them with an empty string.
 * This prevents import errors while keeping tests focused on component logic.
 */
const fileMock = '';

export default fileMock;
